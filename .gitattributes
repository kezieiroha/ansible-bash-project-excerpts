# Always use LF for shell scripts
*.sh text eol=lf

# Optionally apply to other Unix-style scripts
*.bash text eol=lf
*.zsh text eol=lf

# Prevent Git from auto-converting line endings on checkout (optional)
* text=auto

